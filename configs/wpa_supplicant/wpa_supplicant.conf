# wpa_supplicant configuration
# P2P (Wi-Fi Direct) is disabled

# Disable P2P functionality
p2p_disabled=1

# Control interface for NetworkManager
ctrl_interface=/var/run/wpa_supplicant
ctrl_interface_group=wheel
update_config=0
