# ==============================================================================
# Bootstrap Configuration for Alpine Linux on Raspberry Pi
# ==============================================================================
#
# Copy this file to bootstrap.env and modify values for your environment.
# This file contains sensitive information - permissions will be set to 600.
#

# ==============================================================================
# USER CONFIGURATION
# ==============================================================================

# Target user for privilege configuration
TARGET_USER="your_username"

# User full name (GECOS field)
USER_FULL_NAME="Your Full Name"

# User password (will be set for the target user)
USER_PASSWORD="SuperSecureAndStrongPassword"

# SSH public key for the target user (paste your public key here)
USER_SSH_KEY="ssh-ed25519 ... your@email.com"

# SSH password authentication (yes/no)
# Set to "no" to disable password login and allow only SSH key authentication
# WARNING: Only disable password auth if you have configured USER_SSH_KEY above!
SSH_PASSWORD_AUTH="yes"

# ==============================================================================
# WIFI SETTINGS
# ==============================================================================

# WiFi network name (SSID)
WIFI_SSID="Your WiFi Network"

# WiFi password (use single quotes for special characters like $ or !)
WIFI_PASSWORD='YourWiFiPassword'

# WiFi country code (ISO 3166-1 alpha-2)
WIFI_COUNTRY="RO"

# ==============================================================================
# NETWORK INTERFACES
# ==============================================================================
# Leave empty to auto-detect. On Raspberry Pi 5, ethernet is typically "end0",
# WiFi is "wlan0". On Raspberry Pi 4, ethernet is typically "eth0".

ETH_INTERFACE=""
WIFI_INTERFACE=""

# ==============================================================================
# DNS CONFIGURATION
# ==============================================================================

# Search domains (space-separated)
DNS_SEARCH_DOMAINS="local"

# DNS servers - IPv6 (space-separated)
# Default: Cloudflare and Google DNS
DNS_SERVERS_IPV6="2606:4700:4700::1111 2606:4700:4700::1001 2001:4860:4860::8888 2001:4860:4860::8844"

# DNS servers - IPv4 (space-separated)
# Default: Cloudflare and Google
DNS_SERVERS_IPV4="1.1.1.1 1.0.0.1 8.8.8.8 8.8.4.4"

# ==============================================================================
# NTP SERVERS
# ==============================================================================

# NTP servers for time synchronisation (space-separated)
# Default: Cloudflare, Google, and pool.org
NTP_SERVERS="time.cloudflare.com time.google.com pool.ntp.org"

# ==============================================================================
# PACKAGES
# ==============================================================================
# Space-separated list of packages to install.
# Note: fish shell is automatically installed and set as the default shell.
# Docker packages (docker, docker-cli-compose, inotify-tools) are added
# automatically if you choose to set up Docker services.

PACKAGES="rsync fd eza ripgrep bat neovim"
