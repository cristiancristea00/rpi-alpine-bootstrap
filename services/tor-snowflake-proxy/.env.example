# ==============================================================================
# Tor Snowflake Proxy Configuration
# ==============================================================================
#
# Copy this file to .env and modify values for your environment.
#
# This runs a Snowflake proxy to help Tor users connect through WebRTC.
# The proxy uses host networking for maximum connectivity.
#

# Container name
SERVICE_NAME=tor-snowflake-proxy

# Docker image tag (latest or nightly)
IMAGE_TAG=latest