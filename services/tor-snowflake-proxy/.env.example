# ==============================================================================
# Tor Snowflake Proxy Configuration
# ==============================================================================
#
# Copy this file to .env and modify values for your environment.
#
# This runs a Snowflake proxy to help Tor users connect through WebRTC.
# The proxy uses host networking for maximum connectivity and does not require
# port forwarding or special firewall rules.
#

# ==============================================================================
# Container Configuration
# ==============================================================================

# Container name
SERVICE_NAME=tor-snowflake-proxy

# Docker image tag (latest for stable, nightly for development builds)
IMAGE_TAG=latest

# Timezone (TZ database name)
TIMEZONE=Europe/Bucharest