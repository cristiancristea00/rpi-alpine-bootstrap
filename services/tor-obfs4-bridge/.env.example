# ==============================================================================
# Tor obfs4 Bridge Configuration
# ==============================================================================
#
# Copy this file to .env and modify values for your environment.
#
# This runs a Tor bridge with obfs4 pluggable transport to help users
# bypass censorship. Your bridge will help Tor users in censored regions
# connect to the Tor network.
#

# ==============================================================================
# Container Configuration
# ==============================================================================

# Container name
SERVICE_NAME=tor-obfs4-bridge

# Docker image tag (latest for stable, nightly for development builds)
IMAGE_TAG=latest

# Timezone (TZ database name)
TIMEZONE=Europe/Bucharest

# ==============================================================================
# Network Configuration
# ==============================================================================

# Onion Routing port (Tor protocol communication)
# Must be different from PT_PORT and externally reachable
OR_PORT=9001

# Pluggable Transport port (obfs4 obfuscated traffic)
# Choose a port that looks like common traffic (443, 8080, etc.)
# This port must be forwarded through your firewall/router
PT_PORT=443

# ==============================================================================
# Bridge Identity
# ==============================================================================

# Contact email (shown in bridge descriptor, can be pseudonymous)
# Used by Tor Project to contact you if there are issues
EMAIL=your@email.com

# Bridge nickname (1-19 alphanumeric characters, no spaces)
# Choose a unique, memorable name for your bridge
NICKNAME=YourBridgeName

# ==============================================================================
# Advanced Configuration
# ==============================================================================

# Enable additional Tor variables (0 = disabled, 1 = enabled)
# Set to 1 to use the configuration options below
ENABLE_ADDITIONAL_VARIABLES=1

# Publish server descriptor (0 = private bridge, 1 = public bridge)
# Public bridges are listed in BridgeDB and distributed to users
# Private bridges are only shared with people you give the bridge line to
PUBLISH_SERVER_DESCRIPTOR=1

# Bridge distribution method
# Options: none, https, email, telegram, lox, moat, reserved, any
BRIDGE_DISTRIBUTION=any

# Operate as an exit relay (0 = no, 1 = yes)
# WARNING: Exit relays have different legal/technical implications
# Most bridges should NOT be exit relays (keep this at 0)
EXIT_RELAY=0